{
  "name": "whisper-json",
  "version": "0.1.0",
  "description": "NodeJS bindings for Whisper CPP.",
  "main": "index.js",
  "scripts": {
    "start": "echo RUNNING npm run dev && npm run dev",
    "dev": "NODE_ENV='development' nodemon --watch './**/*.ts' --exec 'ts-node' index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "init": "echo INSTALLING AND THEN RUNNING IN DEV && npm i && npm run dev"
  },
  "devDependencies": {
    "@types/node": "^18.11.13",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "cross-spawn": "^7.0.3"
  },
  "author": "Ari",
  "license": "ISC",
  "tags": ["OpenAI", "Whisper", "CPP", "Whisper", "Bindings", "Transcript", "Transcriber", "Audio", "Speech", "Speech-to-Text", "TTS", "SRT"]
}
